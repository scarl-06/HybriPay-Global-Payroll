const { PrismaClient } = require('@prisma/client'); const crypto = require('crypto'); const prisma = new PrismaClient(); async function main() { const keyHash = crypto.createHash('sha256').update('antigravity-secret-key').digest('hex'); await prisma.apiClient.create({ data: { name: 'Frontend App', keyHash: keyHash, role: 'HR' }}); console.log('Created API Key!'); } main().catch(console.error).finally(() => prisma.$disconnect());
